#pragma once
//============================================================================
// 控制台选择菜单 —— 无图形环境时的回退入口
// ---------------------------------------------------------------------------
// 位置：白银纪元/测试层/src/主调/控制台选择菜单.h
// 职责：打印套件编号列表，读取用户输入（all / 1,3-5），
//       把选择结果应用到模型并输出对应的 --gtest_filter 过滤串。
//============================================================================
#include <string>

#include "src/主调/测试选择模型.h"

//引擎命名空间
namespace engine
{
    //运行控制台选择菜单，过滤串写入 过滤串
    //输入流结束或多次非法输入时按「全选」处理
    void 控制台选择菜单_运行(测试选择模型& 模型, std::string& 过滤串);
}
