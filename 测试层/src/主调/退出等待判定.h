#pragma once
//============================================================================
// 退出等待判定 —— 测试结束后是否停住等待用户关窗
// ---------------------------------------------------------------------------
// 位置：白银纪元/测试层/src/主调/退出等待判定.h
// 职责：判断当前这次运行结束后要不要停下来等待按键。
//   判定为假（自动化 / ctest）：带 --gtest_* 且未显式指定 --selector，
//                              或 --selector=off —— 跑完立即返回，不被挂住；
//   判定为真（交互运行）：默认 / window / console —— 停住，等用户看完输出。
// 本文件只有声明：实现位于 主程序.cpp，用例直接链接可执行文件的同名符号，
// 以保证「用例验证的判定」与「主程序实际使用的判定」是同一份逻辑。
//============================================================================
#include <string>

//引擎命名空间
namespace engine
{
    //判断测试跑完后是否需要停住等待用户关闭窗口
    //选择模式：window / console / off
    //命令行带过滤：命令行出现 --gtest_* 参数
    //显式选择器：命令行显式指定了 --selector=
    bool 需要等待退出(const std::string& 选择模式, bool 命令行带过滤, bool 显式选择器);
}