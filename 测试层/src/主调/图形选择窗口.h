#pragma once
//============================================================================
// 图形选择窗口 —— GLFW + Dear ImGui 的临时选择界面
// ---------------------------------------------------------------------------
// 位置：白银纪元/测试层/src/主调/图形选择窗口.h
// 职责：创建一个临时窗口，树形展示套件与用例并支持勾选；
//       用户确认后销毁窗口与 ImGui 上下文，把过滤串交回主调。
// 返回 false 表示窗口创建失败（无图形环境），调用方应退到控制台菜单。
//============================================================================
#include <string>

#include "src/主调/测试选择模型.h"

//引擎命名空间
namespace engine
{
    //运行图形选择窗口
    //成功返回 true，过滤串写入 过滤串（按 Esc 或关闭窗口视为全跑，写入 "*"）
    //窗口创建失败返回 false
    bool 图形选择窗口_运行(测试选择模型& 模型, std::string& 过滤串);
}
