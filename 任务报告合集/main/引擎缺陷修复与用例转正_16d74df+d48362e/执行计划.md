# 引擎层缺陷修复提交 + 测试层用例同步与补充

> 本文件为用户已批准计划的工作站存档副本，对应工作区 `.cyrene/docs/plan-20260925-195447.md`（2026-09-25 批准）。
> 执行结果与偏差说明见同目录 `执行报告.md`。

## 一、目标

1. 把**引擎层工作区当前未提交的接口调整与缺陷修复**提交入库并推送。
2. **同步测试层过时用例**：23 条 `DISABLED_` 用例逐条评估转正，并跟进受影响的接口调用。
3. **补充测试**：本次引擎新行为的用例 + 坐标类型独立用例。

## 二、背景（已完成的事实调查）

### 2.1 引擎层未提交改动（17 文件，实质 16 + 纯空白 1）

| 模块 | 文件 | 实质变化 |
| --- | --- | --- |
| 事件 | `Event/事件.h` | `detail` 命名空间收进 `engine`；哈希特化改用 `engine::detail::` |
| 事件 | `Event_Terminal/core/事件终结.cpp` | `acl_key` 改 `optional`；五个对外接口（attach/interact/send/query/clear）新增"密钥未生成 → `Log::warn` + 返回失败"前置检查；`query` 返回 `const vector*`，失败 `nullptr`；`attach` 补 if/else 分支（原先未注册分支漏调 `attach_handler`）；单事件 `interact` 补 `return true` |
| 事件 | `Event_Terminal/事件终结.h` | 成员与方法签名同步（`std::optional<int64_t> acl_key`、`query` 返回指针） |
| 事件 | `Event_Terminal/框架预声明跨边界使用.h` | 删除多余 `using std::any;` |
| 对象 | `Object_Pool/对象池.h` | 删除 `is_key_integral` 成员；`find` 改 `if constexpr` 编译期分派；`ID()>0` 判定统一为 `valid()`；`build` 的越界分支改为 `is_sorted &&`；`clear` 的 `if(is_sorted)` 修正为 `if(!is_sorted)`；`min_valid_index` 语义由 `nullopt` 改为 `0`；`unload` 补 `ID_set(0)` |
| 空间 | `Quadtree/core/四叉树检索树.hpp` | `max_size` 临时变量 `int → uint64_t`；`child_node_recur` 返回值改用 `entered` 变量，修正 `is_pop_back` 只在成功进入子节点时清除 |
| 空间 | `Quadtree/函数预声明.h` | 删除两行失效注释分隔 |
| 空间 | `Quadtree_Manager/core/四叉树检索树.hpp` | `target_range_amaed` **改名** `target_range_amend`；修正实参顺序为 `(tree_range, target_excel, seekable_range)`；补空指针/除零/越界防御与行列裁剪；`seek` 空序列按 `stable` 决定返回或先建基准树 |
| 空间 | `Quadtree_Manager/core/四叉树合集库.hpp` | `max_expandable_size` 与循环变量 `int → uint64_t`；合并拷贝前补 `ptr_data` 空判定（失败 `continue`） |
| 空间 | `Quadtree_Manager/core/四叉树智能分建.hpp` | `qurdtree_build_smart` 入口补 `coord_set.empty()` 防御 |
| 空间 | `Quadtree_Manager/core/基岩操作.hpp` | 缓存写入段补 `max_cache_records > 0` 判定，判满由 `==` 改为 `>=`；`range_binary_search` 返回无效区间时提前 `return -1` |
| 空间 | `Quadtree_Manager/core/设置与交互.hpp` | `set_min_size` 删除"把下限写进四叉树上限"的循环副作用 |
| 空间 | `Quadtree_Manager/函数预声明.h` | 声明同步改名 |
| 工具 | `Data_Validator/数据校验器.h` | `if constexpr` 链改 `else if constexpr`；新增 `std::string` 分支（非字符串 / 空串拦截）；空值检查限定为 array/object；`error_out` 移至类尾 |
| 工具 | `Data_Validator/core/数据校验器.cpp` | 删除（空文件） |
| 工具 | `Logging/日志系统.h` | **删除全部"文件输出重载"**（`info/warn/error/debug` 带文件名参数的重载）；新增 `stream_set()` 设置 `active_file_stream`；静态成员 `stream_set` 改名 `streams` |
| 空间 | `Collision_Region/core/碰撞器.cpp` | **纯行尾/空白差异**（忽略空白后 0 处），按你的选择一并提交 |

### 2.2 测试层过时点

- **23 条 `DISABLED_` 用例**：四叉树管理器 11、对象池 10、数据校验器 1、事件终端 1。每条的注释里都写明了缺陷位置、成因与修复方向，而本次引擎改动**正是按这些方向修的**。
- **`事件终端测试.cpp`**：`terminal.query(key).size()` 等约 11 处按"返回引用"使用，现需按指针适配。
- **`日志系统测试.cpp`**：`文件输出重载被控制台截走`用例固化的是已被删除的重载行为，前提消失，需按新 `stream_set` 语义重写。
- 用例源由 `file(GLOB_RECURSE ... src/单元测试/*.cpp)` 收集，**新增文件无需改 CMakeLists**。

### 2.3 构建与验证口径

- 构建树：`引擎层/out/build/x64-Debug`、`测试层/out/build/x64-Debug`（Ninja / Debug / MSVC）
- 引擎层构建：`cmake --build 引擎层/out/build/x64-Debug -- -k 0`
- 测试层构建：`cmake --build 测试层/out/build/x64-Debug -- -k 0`
- 全量回归：`测试层/EngineTests.exe --selector=off`（上一版次基线 **287 / 287 通过、21 套件、23 DISABLED**）
- ctest：测试层构建树内 `ctest --output-on-failure`

## 三、任务清单

### A. 引擎层提交

- [ ] A1. 引擎层增量构建（`-- -k 0`）：exit 0、`error C` 计数 0，确认 17 文件改动可编译
- [ ] A2. 提交引擎层 17 文件（含 `碰撞器.cpp` 行尾变更）：`git add --pathspec-from-file`（UTF-8 清单）+ `git commit -F`（UTF-8 信息），回读校验中文与标点
- [ ] A3. 推送引擎层：`git push sliverera HEAD:engine`（本地 `main` 与上游 `engine` 不同名，需显式 refspec）

### B. 测试层过时用例同步

- [ ] B1. `事件终端测试.cpp`：约 11 处 `query()` 调用点由引用语义适配为指针语义（`->size()` / 空指针断言）
- [ ] B2. `事件终端测试.cpp`：`DISABLED_错误密钥查询返回空集合` 转正，按新语义断言返回 `nullptr` 而非空集合
- [ ] B3. `日志系统测试.cpp`：`文件输出重载被控制台截走` 用例按新接口重写（文件重载已删除；改为验证 `stream_set()` 指定活跃流后日志落到文件）
- [ ] B4. `数据校验器测试.cpp`：`DISABLED_布尔字段检查` 转正
- [ ] B5. `四叉树管理器测试.cpp`：11 条 `DISABLED_` 逐条转正（缺陷已修的直接启用；断言与新行为不符的按新语义修正；确实转不了的保留 `DISABLED` 并更新说明）
- [ ] B6. `对象池测试.cpp`：10 条 `DISABLED_` 逐条转正，同上口径
- [ ] B7. 检查其余用例文件（`四叉树测试.cpp`、`事件中转器测试.cpp`、`碰撞测试.cpp` 等）是否受接口变更波及并跟进

### C. 补充测试

- [ ] C1. 新增 `测试层/src/单元测试/核心/坐标类型测试.cpp`：`Point2i`/`Point2d`/`Rect2i`、ULP 相等（浮点 ≤ 4、整数精确）、`point_to_int` 取整规则（`std::lround`）、`Rect2i` 四条边界语义
- [ ] C2. 补事件终端"密钥未生成即锁定"用例：未调用 `acl_key_gen()` 前 attach/interact/send/query/clear 全部被拒绝，`query` 返回 `nullptr`
- [ ] C3. 补数据校验器字符串分支用例：非字符串字段被拦截、空字符串被拦截、合法字符串通过
- [ ] C4. 补日志 `stream_set()` 用例：设置活跃流后日志写入指定文件、未设置时走控制台

### D. 验证

- [ ] D1. 测试层构建（`-- -k 0`）：exit 0、`error C` 计数 0
- [ ] D2. 全量回归 `--selector=off`：记录通过数 / 套件数 / DISABLED 数，与基线 287/21/23 对照，逐项说明增减来源（转正数、新增数）
- [ ] D3. 专项套件确认：`Event_Terminal_Test`、`Object_Pool_Test`、`Data_Validator_Test`、`Quadtree_Manager_Test`、`Log_Test` 全通过
- [ ] D4. ctest：1/1 Passed
- [ ] D5. 转正用例若失败，保留 `DISABLED` + 如实记录原因，不擅自扩大引擎修复范围

### E. 提交与收尾

- [ ] E1. 提交测试层（用例同步 + 新增用例）：同上中文路径提交口径
- [ ] E2. 推送测试层；推送后核对两仓与远端一致
- [ ] E3. 工作站：`任务报告合集/main/引擎缺陷修复与用例转正_<引擎哈希>+<测试哈希>/` 下补 `执行计划.md` + `执行报告.md`
- [ ] E4. 工作站：更新 `报告提交历史.md`、`项目导航合集/main/分支提交历史.md`，并新增本版次项目导航目录
- [ ] E5. 清理本阶段临时文件（脚本 / 日志 / 中间产物）

## 四、风险与回退

| 风险 | 处置 |
| --- | --- |
| 部分 `DISABLED_` 用例的缺陷并未被本次改动完整覆盖，转正后失败 | 不硬改断言迁就现状：保留 `DISABLED_` 前缀、更新注释说明现状与阻塞点，并在报告中单列 |
| 转正后暴露新的引擎缺陷 | 不擅自扩大修复范围，先如实上报，由你决定是否纳入下一版次 |
| 日志系统删除了重载，可能有测试外的调用点未发现 | 构建时以 `error C` 全量收集为准（`-k 0` 不中断），逐个跟进 |
| 引擎层推送被 `push.default=simple` 拦下 | 沿用显式 refspec `git push sliverera HEAD:engine`，非强制推送 |
| 中文路径在 cmd 下乱码 | 沿用 `--pathspec-from-file`（UTF-8 清单）+ `commit -F`，提交后回读校验 |
| 碰撞器.cpp 行尾变更入库 | 你已确认一并提交；提交后该文件字节由 LF 变为 CRLF，将如实记录在报告里 |
| 全量回归数字与基线不一致 | 逐项归因（转正 / 新增 / 禁用），确认无既有用例回退 |

## 五、执行约束

- 全程**不使用子代理**（你的机器内存不足）
- 只动引擎层与测试层；`系统层`、`游戏层` 不触碰
- 引擎层改动中你其他的工作区内容（如有）不擅自纳入提交，暂存清单显式指定路径
