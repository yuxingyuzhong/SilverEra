# 碰撞系统 · 模块架构

> 版次：`引擎层 089b070`（`main` → `engine`）
> 前序版次：引擎层 `fa8459a`
> 本版次主题：**碰撞模块单文件拆分为多编译单元**——`Collision_Proxy` 与 `Collision_Region` 两个大单文件（919 行 / 839 行）按职责拆为 9 个编译单元，纯搬运重构，对外行为零变化。
> 文件定位：本文件是「碰撞系统」模块的「模块架构」文档，覆盖 `src/core/spatial/collision/` 下全部内容（`Collider/`、`Collision_Region/`、`Collision_Proxy/`）及 `src/core/spatial/common/` 下的依赖库封装与碰撞系统运行包。
> 依据《agent开发指南·项目导航合集/目录细则》<2> 建立：(1) 模块功能、(2) 模块提供接口、(3) 模块内部架构、(4) 模块设计/实现细节、(5) 模块运行链路。

---

## 一、模块功能

碰撞系统是引擎层 `core/spatial/collision/` 下的一组**空间碰撞检测能力**，为上层提供「碰撞空间（世界）— 碰撞体（对象）— 碰撞代理器（门面）」三层结构，基于 **bullet3**（`external/bullet3`）物理库的碰撞检测子集（仅碰撞，不含动力学求解）实现。

### 1.1 解决的三个问题

1. **碰撞体建模**：把游戏/系统层的对象抽象为可参与碰撞检测的 `Collider`，支持六种几何形状（盒体 / 球体 / 胶囊 / 圆柱 / 圆锥 / OBJ 网格），可配置位移向量、检测方式（扫掠/离散）、豁免标记。
2. **碰撞世界管理**：把碰撞体按「碰撞空间（`Collision_Region`）」分组管理，每个空间有独立的 bullet3 碰撞世界后端（`Collision_Backend`）、独立的编号分配器与空间边界（静态网格）。
3. **对外统一门面与事件接入**：`Collision_Proxy` 是模块对外的门面，统一管理多个碰撞空间、维护「碰撞体编号 → 归属空间」的全局映射，并将全部操作接入事件系统（订阅 `Config` / `Collision` 两分类 11 条指令，发布 `DetectResult` / `ColliderBuildResult` 结果事件）。

### 1.2 服务对象

- 引擎层内部：被 `碰撞系统运行包.h` 聚合导出；
- 上层消费方：系统层、测试层通过 `Collision_Proxy` 构建空间/碰撞体、执行检测、读取碰撞结果；
- 测试层：`碰撞测试.cpp` 直接构造 `Collision_Proxy` 验证全部接口（`Collision_Proxy_Test` 8 例、`Collision_Region_Test` 9 例、`Collider_Test` 2 例）。

### 1.3 本版次定位

`089b070` 之前碰撞系统只有两个巨大实现文件（`碰撞代理器.cpp` 919 行、`碰撞空间.cpp` 839 行），本版次按职责拆分为 9 个编译单元，函数体/注释/日志文本逐字未改，两个 `.h` 与两个 `局部命名空间使用.h` 一字未动，对外符号集 diff 零差异。

---

## 二、模块提供接口

### 2.1 Collider（`Collider/碰撞体.h`）

```cpp
//碰撞检测方式
struct Detection_Mode
{
    bool is_swept_volume = false;   //扫掠检测标记
    uint32_t step_length = 0;       //离散检测步长
};

//碰撞体
struct Collider
{
    uint64_t ID = 0;                                //编号
    Collision_Object object;                        //碰撞对象（bullet3，经依赖库封装别名）
    std::unique_ptr<Triangle_Mesh> mesh;            //网格接口（网格形状的数据源，成员先声明后析构）
    std::unique_ptr<Collision_Shape> shape;         //几何形状（碰撞体持有，碰撞对象只挂载指针）
    Vector3 displacement_vector{0.0f,0.0f,0.0f};    //位移向量
    Detection_Mode detection_mode;                  //检测方式
    uint64_t exemption_flag = 0;                    //豁免标记（同组豁免的碰撞对跳过）
    nlohmann::json geometry;                        //几何配置（供空间间镜像与转移复用）

    Collider();                                     //默认构造：object.setUserPointer(this)
    ~Collider() = default;
    Collider(const Collider&) = delete;             //禁止拷贝（object 加入 world 后不能搬移）
    Collider& operator=(const Collider&) = delete;
    Collider(Collider&& other) noexcept;            //移动构造：搬移后重新挂载自指针 + 清空源自指针
    Collider& operator=(Collider&& other) noexcept;
    static Collider* recover(const Collision_Object* obj);  //反查：从碰撞对象取回封装结构体
};
```

要点解释：
- **自指针机制**：`Collider` 构造时 `object.setUserPointer(this)`，bullet3 在检测回调/流形中给出碰撞对象地址，`Collider::recover()` 借 `getUserPointer()` 反查回碰撞体。移动后必须重新挂载（默认移动会把自指针指向被搬空的源对象）。
- **析构顺序**：`mesh` 先于 `shape` 声明，按成员声明逆序析构，故析构时形状先释放、网格后释放——`btBvhTriangleMeshShape` 只持网格指针不接管所有权，网格必须与形状同生共死。

### 2.2 Collision_Region（`Collision_Region/碰撞空间.h`）

```cpp
//碰撞结果
struct Collision_Result
{
    uint64_t collider_A = 0;    //碰撞体A（归一化后较小者在前）
    uint64_t collider_B = 0;
};

//碰撞空间
struct Collision_Region
{
    Collision_Region();                                   //默认构造（名称空串）
    explicit Collision_Region(const std::string& region_name);
    Collision_Region(const Collision_Region&) = delete;   //禁止拷贝（backend 含 unique_ptr）
    Collision_Region& operator=(const Collision_Region&) = delete;
    Collision_Region(Collision_Region&&) = default;       //默认移动（边界搬移有悬空指针陷阱，见文档四）
    Collision_Region& operator=(Collision_Region&&) = default;
    ~Collision_Region();                                  //析构前先把全部碰撞对象移出碰撞世界

    bool valid(void) const;                               //后端有效性检查
    void state_set(bool active);                          //空间活跃性设置
    bool state_check() const;                             //空间活跃性查看
    bool boundary_build(const std::string& mesh_path);    //边界构建（静态网格）
    void boundary_unload();                               //边界卸载

    uint64_t collider_build(void);                        //碰撞体构建（分配编号）
    bool collider_unload(uint64_t collider_ID);           //碰撞体卸载
    bool collider_set(const uint64_t collider_ID, const Vector3& vector);        //位移向量
    bool collider_set(const uint64_t collider_ID, const Detection_Mode& vector); //检测方式
    bool collider_set(const uint64_t collider_ID, const uint64_t& vector);       //豁免标记
    bool collider_set(nlohmann::json geometry_config);    //几何体添加（box/sphere/capsule/cylinder/cone/mesh）
    bool collider_find(uint64_t collider_id) const;       //碰撞体查询
    std::vector<uint64_t> colliders(void);                //碰撞体全量获取
    bool collider_adopt(uint64_t collider_ID);            //碰撞体接管（按指定编号登记，供空间间转移）
    nlohmann::json collider_geometry(uint64_t collider_ID) const;  //几何配置读取（不存在返回空 json）

    std::optional<std::vector<Collision_Result>> detect(void);  //执行碰撞检测（扫掠+离散两阶段）
    bool contains(uint64_t collider_ID) const;            //碰撞体包含性检测（是否已进入碰撞世界）
};
```

要点解释：
- `detect()` 返回 `std::optional`：空间无效或未激活时返回 `std::nullopt`（空 optional 而非空 vector），调用方据此区分「未执行检测」与「执行了但无碰撞」。
- `collider_adopt()` 供空间间转移使用：按原编号在目标空间登记（编号冲突时失败，源空间尚未改动）。

### 2.3 Collision_Proxy（`Collision_Proxy/碰撞代理器.h`）

```cpp
class Collision_Proxy
{
public:
    Event_Terminal event_terminal;   //事件终端（对外暴露，供外部直接操作）

    Collision_Proxy();               //构造：event_terminal.acl_key_gen() 生成权限密钥
    ~Collision_Proxy() = default;

    //---- 空间管理 ----
    bool region_build(const std::string& region);             //碰撞空间构建
    bool region_unload(const std::string& region);            //碰撞空间卸载
    bool region_detect(const std::string& region);            //碰撞空间检测执行（结果走事件发布）
    bool region_boundary_set(const std::string& path);        //碰撞空间边界设置（读 JSON 配置）
    bool region_state_set(const std::string& region, bool active);  //碰撞空间活跃性设置

    //---- 碰撞体管理 ----
    std::optional<uint64_t> collider_build(const std::string& region);              //碰撞体构建
    bool collider_unload(const uint64_t collider_ID, const std::string& region);    //碰撞体卸载
    bool collider_transfer(const uint64_t collider_ID, const std::string& region);  //碰撞体转移
    bool collider_mirror(const uint64_t collider_ID, const std::string& region);    //碰撞体镜像
    bool collider_set(const uint64_t collider_ID, const Vector3& vector);           //位移向量
    bool collider_set(const uint64_t collider_ID, const Detection_Mode& vector);    //检测方式
    bool collider_set(const uint64_t collider_ID, const uint64_t& vector);          //豁免标记

    void attach(void);   //事件中转站接入（订阅 Config/Collision 两分类 11 条指令）
};
```

要点解释：
- **归属映射**：`std::unordered_multimap<uint64_t, std::string> collider_mapping` 记录「碰撞体编号 → 持有空间名称」；编号由各空间自行分配，**同一编号可被不同空间分别持有**（镜像场景），故用多重映射。`collider_set_*` 对所有持有该编号的空间生效。
- **事件接入**：`attach()` 需先经 `event_terminal->interface_check(ATTACH_HANDLER)` 确认事件中转站接入入口已注册；成功后注册事件接收入口，并提交 11 条订阅事件（详见文档五）。
- **事件结果回告**：由于事件调用无法取得返回值，`ColliderBuild` 指令处理完成后以 `ColliderBuildResult` 事件回告新编号；`RegionDetect` 处理完成后以 `DetectResult` 事件广播碰撞对。

---

## 三、模块内部架构

### 3.1 文件组织（本版次拆分后）

```
src/core/spatial/collision/
├── Collider/
│   └── 碰撞体.h                     # Detection_Mode + Collider（纯头，无实现文件）
├── Collision_Region/
│   ├── 碰撞空间.h                   # Collision_Result + Collision_Region 声明
│   ├── 局部命名空间使用.h           # 展开 string/unique_ptr 等 using 别名
│   └── core/                        # ← 本版次由 碰撞空间.cpp(839) 拆出
│       ├── 空间与边界.cpp(108)      # 构造/析构/有效性/状态/边界构建与卸载
│       ├── 碰撞体管理.cpp(279)      # 碰撞体构建/接管/卸载/设置/查询/全量/几何读写/包含性
│       ├── 检测执行.cpp(174)        # detect()：扫掠 + 离散两阶段 + 归一化 + 去重
│       └── 形状构建.cpp(306)        # shape_build/mesh_shape_build + scalar/vector/quaternion_read
└── Collision_Proxy/
    ├── 碰撞代理器.h                 # Collision_Proxy 声明
    ├── 局部命名空间使用.h
    └── core/                        # ← 本版次由 碰撞代理器.cpp(919) 拆出
        ├── 空间管理.cpp(166)        # 构造/空间构建/卸载/检测/状态/查找/归属映射
        ├── 空间边界与配置.cpp(177)  # region_boundary_set/config_apply/boundary_config_read
        ├── 碰撞体管理.cpp(200)      # 构建/卸载/转移/镜像/设置三重载
        ├── 碰撞体配置.cpp(153)      # collider_set_dispatch/collider_config_apply
        └── 事件交互.cpp(272)        # attach/event_process/event_publish
```

### 3.2 依赖关系（include 拓扑）

```
碰撞代理器.h
  ├── common/前置头文件包含.h            # 预编译头（标准库 + nlohmann::json + GLFW）
  ├── src/tools/Engine_Env/引擎环境.h
  ├── src/tools/Auxi_Algorithm/路径字符串转换.h
  ├── ../../common/core/依赖库封装.h      # bullet3 别名 + Collision_Backend
  ├── ../../common/碰撞系统运行包.h       # 依赖库封装 + 碰撞体 + 碰撞空间
  └── src/core/event/事件系统运行包.h     # 事件 + 事件终端

碰撞空间.h
  ├── 前置头文件包含.h
  ├── ../../common/core/依赖库封装.h
  ├── ../Collider/碰撞体.h
  └── src/tools/Number_Allocator/数值分配器.h

各 core/*.cpp
  ├── ../局部命名空间使用.h
  └── 按需 include：日志系统.h / 数据校验器.h / 网格加载器.h
```

### 3.3 数据成员（三个类的核心状态）

**Collision_Proxy**：
- `inline static const std::string module_name = "Collision_Proxy";` —— 事件中转站登记名与事件发送者标识；
- `std::unordered_map<std::string, std::unique_ptr<Collision_Region>> regions;` —— 空间集合（按名索引，unique_ptr 持有）；
- `std::unordered_multimap<uint64_t, std::string> collider_mapping;` —— 编号归属多重映射；
- `Event_Terminal event_terminal;`（公开）与 `int64_t acl_key = 0;`（私有）。

**Collision_Region**：
- `std::string name;`（空间名）、`bool is_active = false;`（活跃性）；
- `Number_Allocator ID_allocator;`（编号分配器，编号 0 预留给空间边界）；
- `Collider region_boundary;`（空间边界，直接成员，静态网格形状）；
- `Collision_Backend backend{};`（bullet3 后端：config/dispatcher/broadphase/world）；
- `std::unordered_map<uint64_t, Collider> mapping;`（碰撞体映射，按值持有 Collider）。

**Collider**：见 2.1（ID / object / mesh / shape / displacement_vector / detection_mode / exemption_flag / geometry）。

### 3.4 依赖库封装（`common/core/依赖库封装.h`）

```cpp
using Collision_Config     = btDefaultCollisionConfiguration;  //碰撞配置别名
using Collision_Dispatcher = btCollisionDispatcher;            //窄阶段检测调度器
using Collision_Interface  = btBroadphaseInterface;            //宽阶段检测接口
using Bvh_Tree             = btDbvtBroadphase;                 //BVH 宽阶段
using SAP                  = btAxisSweep3;                     //扫描线宽阶段
using Collision_World      = btCollisionWorld;                 //碰撞世界
using Collision_Object     = btCollisionObject;                //碰撞对象
using Bvh_Triangle_Mesh    = btBvhTriangleMeshShape;           //BVH 三角形网格形状
using Vector3 = btVector3;  using Transform = btTransform;
using Quaternion = btQuaternion;  using Scalar = btScalar;
using Collision_Shape = btCollisionShape;  using Convex_Shape = btConvexShape;
using Box_Shape / Sphere_Shape / Capsule_Shape / Cylinder_Shape / Cone_Shape;
using Triangle_Mesh = btTriangleMesh;  using Persistent_Manifold = btPersistentManifold;
using Swept_Callback = Collision_World::ConvexResultCallback;
using Swept_Result   = Collision_World::LocalConvexResult;
inline constexpr int Static_Object_Flag = btCollisionObject::CF_STATIC_OBJECT;
```

`Collision_Backend` 结构体持有 config（shared_ptr 共享）+ dispatcher/broadphase/world（各自 unique_ptr，`new(nothrow)` 分配，任一失败全部回滚），提供 `valid()` 检查。

---

## 四、模块设计/实现细节

### 4.1 本版次拆分（089b070）的搬运口径与经验

- **搬运原则**：函数体、注释、`Log::warn/error/debug` 文本逐字未改；每个单元只加最小 include（`../局部命名空间使用.h` ＋ 实际用到的工具头）；匿名命名空间辅助随其使用者同文件，**未提升任何辅助的可见性**。
- **一次分组假设被实测推翻**：原计划假设 `text_read` / `number_read` 只服务单一使用者，实测发现 `event_process` 也在用（共 8 处）。匿名命名空间实体跨 TU 不可见 → 构建报 8 条 `C3861`。按「不提高辅助可见性」原则，改为由 `事件交互.cpp` 自带副本（两份独立副本，消重需引入模块内部共享头，本版次未做）。
- **一次补丁事故**：插入辅助时漏带匿名命名空间闭合 `}`，`attach` / `event_process` / `event_publish` 被卷入匿名命名空间（6 条 `C2888` / `C1075`）→ 放弃增量修补，修正生成脚本后整批重生成 9 个文件。
- **`memory_malloc` 归属**：`形状构建.cpp` 的 `memory_malloc` 被两个函数共用且为匿名实体，外移会让接收方越界或语义错位，故保留在 306 行文件内（超计划 300 行上限 6 行）。
- **验证**：`EngineCore.lib` 外部符号集前后均 16342 条、`diff` 零差异；`C4005` 由 12 → 19（每编译单元各报一次，编译单元数 120 → 127 一一对应）。

### 4.2 碰撞空间生命周期（关键陷阱）

1. **构造**：`Collision_Region(name)` 记录名称，并 `region_boundary.ID = ID_allocator.get()` **预分配编号 0 恒保留给空间边界**。
2. **析构**：碰撞体与空间边界都直接持有碰撞对象（`Collision_Object`），而碰撞世界内部记录的是这些对象的**地址**；碰撞体映射先于碰撞世界析构，故 `~Collision_Region()` 必须先把全部碰撞对象移出碰撞世界（`world->removeCollisionObject`），否则碰撞世界析构时解引用已释放对象（3 例夹具崩溃即源于此）。
3. **移动陷阱**：`Collision_Region` 默认移动构造/赋值被保留，但**碰撞世界内部记录的是碰撞对象地址，而空间边界是直接成员**——向碰撞世界挂入边界后再按值搬移本结构体会留下悬空指针。`Collision_Proxy` 按 `unique_ptr` 搬移不受影响；确需按值搬移时须搬移后重新执行边界构建（`boundary_build`）。

### 4.3 碰撞体配置与形状构建（`形状构建.cpp`）

- **形状类型**：`box`（half_extent 三元数组，各分量须 >0）、`sphere`（radius>0）、`capsule`（radius>0 且 height ≥ 2*radius，沿 Y 轴，bullet 第二参数为圆柱段长度 = height-2*radius）、`cylinder`（radius>0，沿 Y 轴以半长描述，`Vector3(r, h*0.5, r)`）、`cone`（radius>0、height>0，沿 Y 轴）、`mesh`（mesh_path）。
- **碰撞边距**：缺省 `margin = 0.04`（bullet 默认），显式指定时须 ≥0；`shape_receiver->setMargin()` 下发（盒体/圆柱/胶囊会自行修正隐式尺寸以保持总体尺寸不变）。
- **字段读取三件套**：`scalar_read`（浮点或整数皆可读入 double）、`vector_read`（三元数组，`field_check<std::vector<double>>` + 长度 3）、`quaternion_read`（四元数组，按 x/y/z/w 顺序 `setValue`）。
- **网格形状**：`mesh_shape_build` 经 `Mesh_Loader::load_obj` 加载 → `memory_malloc<Triangle_Mesh>(true, true)`（32 位索引 + btVector3 顶点）→ 预分配容量 → `findOrAddVertex(v, false)`（关闭查重，按调用顺序追加，下标与网格数据一一对应）逐顶点写入 → `addTriangleIndices` 写索引 → `memory_malloc<Bvh_Triangle_Mesh>(mesh_receiver.get(), true, true)`（BVH 形状，数据源由调用方一并持有；分配失败时重置 mesh）。
- **`memory_malloc` 模板**：bullet 形状类经 `BT_DECLARE_ALIGNED_ALLOCATOR` 只声明抛异常形式的内存分配算子，无法 `new(nothrow)`；故统一包成 try/catch 返回 `bool` 的模板，失败 `Log::warn`。

### 4.4 碰撞检测两阶段算法（`检测执行.cpp`）

`Collision_Region::detect()` 的执行流程：

1. **前置**：`!valid() || !is_active` 时返回 `std::nullopt`；否则 `world->updateAabbs()` 刷新包围盒（挂入后尚未执行过检测时需要）。
2. **扫掠阶段**（位移途中命中）：
   - 遍历 `mapping`，仅处理「启用扫掠检测 + 已挂载凸包形状 + 位移向量非零」的碰撞体；
   - 以 `sweep_from`（当前变换）→ `sweep_to`（当前 + 位移向量）执行 `world->convexSweepTest`；
   - `Swept_Collector` 收集器**固定返回未命中分数 1.0f**，保证收集全部命中而非最近一次；跳过发起扫掠的碰撞对象自身；命中对象经 `Collider::recover()` 反查；同组豁免（exemption_flag 相等且非零）跳过；
   - 结果经 `pair_normalize` 归一化（较小编号在前）入 `pairs`。
3. **离散阶段**（就地平移后求交叉接触）：
   - 遍历 `mapping`，对位移非零的碰撞体：`transform.setOrigin(origin + displacement)` 就地平移 → **清零位移向量（位移一次性生效）**；
   - `world->performDiscreteCollisionDetection()` → 遍历 `dispatcher->getManifolds()`，`getNumContacts() == 0` 跳过；两侧 `Collider::recover()` 反查；同组豁免跳过；归一化入 `pairs`。
4. **去重**：`pair_unique` 用 `std::set<std::pair<uint64_t,uint64_t>>` 保留首次出现的碰撞对。

**注意**：检测会**实际修改**碰撞体位移（清零）——`region_detect` 是消费型调用，重复调用同一位移不会再次平移。

### 4.5 代理器的转移 / 镜像语义

- **转移 `collider_transfer`**：查归属空间 → 取源空间几何配置 `collider_geometry` → 目标空间 `collider_adopt(collider_ID)`（原编号接管，冲突即中止）→ 源空间 `collider_unload`（失败则回滚目标空间接管）→ 更新归属映射 → 有几何配置时 `geometry["collider_ID"]=原编号` 后 `collider_set` 重建几何体。**无几何配置时仅转移编号登记**（占位碰撞体）。
- **镜像 `collider_mirror`**：取源几何配置 → 目标空间 `collider_build`（全新编号）→ 有几何配置时按新编号重建。源碰撞体保留，两空间各持一份。

### 4.6 配置驱动的空间/碰撞体构建（`空间边界与配置.cpp`）

`config_apply(config)` 处理 `Config/Load` 路由指令（`target_object == "Collision_Proxy"`）：
- 顶层须为 object 且含 `regions` 数组；
- 每个 region 配置：`region`（名称）→ `region_build` → 可选 `active`（活跃性）→ 可选 `boundary`（网格路径 → `boundary_build`）→ 可选 `colliders` 数组（逐个 `collider_build` + `collider_config_apply`）；
- `collider_config_apply` 支持四字段：`geometry`（对象，补 `collider_ID` 后 `collider_set`）、`displacement`（三元数组）、`detection_mode`（`is_swept_volume` 布尔 + `step_length` 整数）、`exemption_flag`（整数）。

`region_boundary_set(path)` 读 JSON 数组，每条含 `region` + `mesh_path`，逐空间 `boundary_build`；全部成功才返回 true（部分失败记 `all_set=false` 并 `Log::warn`）。

### 4.7 事件接入与处理（`事件交互.cpp`）

`attach()` 的三步校验链：
1. `event_terminal->interface_check(interface_ID::ATTACH_HANDLER)`——事件中转站接入入口未注册则中止；
2. `event_terminal->event_receiver_register([this](shared_ptr<event> evt){ this->event_process(evt); })`——注册事件接收入口；
3. `event_terminal.attach(module_name, needed_events, acl_key)`——提交订阅清单并接入。

`needed_events` 订阅清单（11 条，全部 `sender/target` 置空、category/tag 成对）：
| category | tag |
| --- | --- |
| Config | Load |
| Collision | RegionBuild / RegionUnload / RegionState / RegionBoundary / RegionDetect |
| Collision | ColliderBuild / ColliderUnload / ColliderTransfer / ColliderMirror / ColliderSet |

`event_process(evt)` 分派：
- `Config/Load`：`evt->target_object != module_name` 时忽略（**定向指令**），否则 `config_apply`；
- `Collision/RegionBuild`：读 `region` → `region_build`；
- `Collision/RegionUnload`：读 `region` → `region_unload`；
- `Collision/RegionState`：读 `region` + `active` → `region_state_set`；
- `Collision/RegionBoundary`：读 `path` → `region_boundary_set`；
- `Collision/RegionDetect`：读 `region` → `region_detect`；
- `Collision/ColliderBuild`：读 `region` → `collider_build` → 发布 `ColliderBuildResult`（`region` + `collider_ID`）；
- `Collision/ColliderUnload/Transfer/Mirror/Set`：读 `region` + `collider_ID` → 对应处理（Set 走 `collider_config_apply`）；
- 其余 category/tag：`Log::debug` 未识别。

`event_publish(tag, payload)`：`event_terminal.build(module_name, "", "Collision", tag)` → 写载荷 → `event_terminal.send(evt, acl_key)`（单事件通道）。

---

## 五、模块运行链路

### 5.1 典型运行链路一：配置驱动初始化（事件接入）

```
Config_Loader::act()
  └─ send(Config/Load, target=Collision_Proxy, config={regions:[...]})
       └─ Event_Broker 定向投递（target_object 非空 → 直投 event_entries[Collision_Proxy]）
            └─ Collision_Proxy::event_process()
                 └─ config_apply(config)
                      ├─ region_build(每个 region)          → Collision_Proxy 构造 Collision_Region
                      ├─ region_state_set / boundary_build   → 空间边界静态网格
                      └─ collider_build + collider_config_apply
                           ├─ Collision_Region::collider_build()   → ID_allocator.get() 分配编号
                           └─ Collision_Region::collider_set(geometry) → shape_build → world->addCollisionObject
```

### 5.2 典型运行链路二：检测执行与结果分发

```
游戏逻辑调用（直接或经事件）
  └─ Collision_Proxy::region_detect("scene")
       ├─ region_seek("scene") → Collision_Region*
       └─ target->detect()
            ├─ updateAabbs()
            ├─ 扫掠阶段：convexSweepTest + Swept_Collector → pairs
            ├─ 离散阶段：就地平移 + performDiscreteCollisionDetection → pairs
            └─ pair_unique → vector<Collision_Result>
       ├─ 组装 payload{region, results:[{collider_A,collider_B},...]}
       └─ event_publish("DetectResult", payload) → Event_Broker 广播
            └─ 订阅 Collision/DetectResult 的模块（如游戏逻辑）消费碰撞对
```

### 5.3 典型运行链路三：碰撞体转移 / 镜像

```
Collision_Proxy::collider_transfer(ID, "target")
  ├─ collider_owner_seek(ID, owner)          → 找到归属空间
  ├─ source->collider_geometry(ID)           → 取几何配置
  ├─ target->collider_adopt(ID)              → 原编号接管（冲突中止）
  ├─ source->collider_unload(ID)             → 失败则回滚 target 接管
  ├─ collider_mapping_unlink + link          → 更新归属映射
  └─ 有几何时 target->collider_set(geometry) → 重建几何体

Collision_Proxy::collider_mirror(ID, "target")
  ├─ source->collider_geometry(ID)           → 取几何配置
  ├─ collider_build("target")                → 全新编号 mirror_ID
  └─ 有几何时 target->collider_set(geometry with mirror_ID)
```

### 5.4 与其它模块的协作清单

| 协作方 | 协作内容 |
| --- | --- |
| 事件系统（Event_Terminal / Event_Broker） | attach 订阅 11 条指令；event_process 处理；event_publish 发布 DetectResult / ColliderBuildResult |
| 数值分配器（Number_Allocator） | Collision_Region 碰撞体编号分配/回收；编号 0 预留给空间边界 |
| 网格加载器（Mesh_Loader） | 空间边界与 mesh 形状的 OBJ 加载 |
| 数据校验器（Data_Validator） | 几何配置字段校验（field_check）、边界配置路径校验（path_check） |
| 日志系统（Log） | 全部错误/警告/调试输出 |
| 路径字符串转换（string_to_path） | 中文配置路径转换为 std::filesystem::path |
| 引擎环境（Engine_Env） | 网格路径的绝对路径兜底 |
| bullet3（依赖库封装） | Collision_Backend 世界 / 形状 / 检测 / 流形 |

---

## 六、边界情况与易踩坑点汇总

| # | 场景 | 行为 |
| --- | --- | --- |
| 1 | `region_build` 重复构建同名空间 | `Log::warn` + 返回 false，不覆盖 |
| 2 | `region_unload` 不存在的空间 | `Log::warn` + false；卸载前先注销该空间全部碰撞体编号归属 |
| 3 | `region_detect` 空间不存在 / 未激活 | false / `std::nullopt`，不发布结果事件 |
| 4 | `collider_build` 空间不存在 | `Log::warn` + `std::nullopt` |
| 5 | `collider_unload` 编号不存在 | false；存在形状时先摘除再注销 |
| 6 | 转移编号在目标空间冲突 | `collider_adopt` 失败，转移中止（源空间未改动） |
| 7 | 转移时源卸载失败 | 回滚目标空间接管 |
| 8 | 占位碰撞体（无几何配置）转移 | 仅转移编号登记 |
| 9 | 盒体半长非正 / 胶囊全高不足 | `Log::warn` + 构建失败 |
| 10 | 网格文件超 64MiB / 顶点超 100 万 | `Mesh_Loader` 拒绝 |
| 11 | 同组豁免（exemption_flag 相等且非零） | 扫掠与离散两阶段均跳过该碰撞对 |
| 12 | `Collision_Region` 按值搬移（已挂边界后） | 悬空指针——须搬移后重做 `boundary_build` |
| 13 | 析构期 | 必须先把碰撞对象移出碰撞世界，否则解引用已释放对象 |
| 14 | `detect()` 消费型 | 会清零碰撞体位移向量，重复调用不重复平移 |
| 15 | 边界配置 JSON 非数组 / 缺字段 | `Log::warn` + 部分失败（`all_set=false`） |
| 16 | `event_process` 收到空事件 | 直接 return |
| 17 | 事件中转站接入入口未注册 | `attach()` 中止并 `Log::error` |

---

## 七、本版次验证记录（089b070）

- 引擎层构建 exit 0 / `error C` 0，`EngineCore.lib` 产出（`[128/129] Linking CXX static library`）；
- 测试层构建 exit 0；
- `EngineTests.exe --selector=off`：**338 / 338 通过 / 0 failed / 0 DISABLED**（22 套件，exit 0），其中 `Collision_Region_Test` 9 例、`Collision_Proxy_Test` 8 例、`Collider_Test` 2 例全 OK；
- `EngineCore.lib` 外部符号集前后均 16342 条、`diff` 零差异（同规则双向复现）；
- 告警：C4715 ×3、C4828 ×2 与基线逐条一致；C4005 12 → 19（与编译单元数 120 → 127 对应，既知同源告警）。

---

## 八、遗留与约定

1. `形状构建.cpp` 306 行、超计划 300 行上限 6 行未挪动（`memory_malloc` 为匿名实体且被两函数共用）；
2. 9 个单元行数未二次匀化（`空间与边界.cpp` 108 行偏短、`形状构建.cpp` 306 行偏长）；
3. `事件交互.cpp` 的 `text_read` / `number_read` 与 `空间边界与配置.cpp`、`碰撞体配置.cpp` 中的同名实体为**独立副本**（消重需引入模块内部共享头，本版次未做）；
4. 空间边界编号恒为 0（`ID_allocator` 预分配），对外文档应明确该约定；
5. 扫掠检测 `step_length` 字段目前仅存储，检测执行未按步长细分（当前为单段凸包扫掠）；
6. 上一版次遗留：`Quadtree/core/区块信息检索.hpp(72)` 与 `Event_Broker/core/事件中转器.cpp(221)` 的既有 `warning C4715` 与本模块无关，在案未处理。
