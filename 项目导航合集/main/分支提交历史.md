# main 分支提交历史

> 用途：记录 main 分支代码提交哈希的时间顺序，及每一版次代码的新增功能。
> 更新规则：每次建立新代码版次的「项目导航」文件夹时同步更新本文件。
> 当前最新版次（代码推送哈希）：55e48cd7a5d441f850f1734be8c43a48f00ceb4d

---

## 仓库基本信息

- 顶层仓库：`https://github.com/yuxingyuzhong/SilverEra.git`
- 当前分支：`main`
- 远端分支：`main`、`system`、`engine`、`game`、`test`、`gitignore`
- 工作区结构：`测试层/`、`系统层/`、`引擎层/`、`游戏层/`
  - 四层各自为**独立 git 仓库**，顶层仓库以嵌套方式统辖它们
  - 因此顶层仓库的提交只反映各层的子模块/目录指针变化，各层自身的提交历史需分别在其仓库内查看

---

## 2026-09-19（当前版次）

- 提交哈希：`55e48cd7a5d441f850f1734be8c43a48f00ceb4d`
- 提交信息：Merge pull request #1 from yuxingyuzhong/gitignore
- 工作区状态：干净，与 `origin/main` 一致
- 本版次由 5 笔提交构成（按时间顺序）：

| # | 提交哈希 | 时间 | 提交信息 |
| --- | --- | --- | --- |
| 1 | `716d94f` | 2026-09-19 08:54 | Initial commit |
| 2 | `b54b725` | 2026-09-19 09:08 | 白银纪元顶层仓库初始化 |
| 3 | `de2686d` | 2026-09-19 09:50 | 顶层仓库：第三方 googletest 副本移出版本管理 |
| 4 | `dd4a919` | 2026-09-19 09:58 | 顶层忽略规则补入测试层 |
| 5 | `55e48cd` | 2026-09-19 09:59 | Merge pull request #1 from yuxingyuzhong/gitignore |

- 新增功能/变更主体：
  - 白银纪元顶层仓库建立，形成「测试层 / 系统层 / 引擎层 / 游戏层」四层结构；
  - 第三方 googletest 副本移出版本管理，改由忽略规则排除；
  - 顶层忽略规则补入测试层；
  - 忽略规则分支 `gitignore` 通过 PR #1 合入 `main`。
- 项目导航：**未建立**。

---

## 2026-09-25（测试层主调文件：测试模块选择器）

- 顶层仓库版次：**本版次无新提交**（顶层仓库最新仍为 `55e48cd7a5d441f850f1734be8c43a48f00ceb4d`；四层为独立仓库，顶层仓库不跟踪其内容）
- 代码版次（`测试层` 子仓库，分支 `test`；远端 `sliverera/test`）：

| # | 提交哈希 | 提交信息 |
| --- | --- | --- |
| 1 | `0555060` | 测试层：新增测试模块选择器（图形窗口 + 控制台菜单）｜28 files changed / +76701 −7 |
| 2 | `272c44f` | 测试层：跟进引擎层 Config_Checker → Data_Validator 重命名｜1 file changed / +49 −49 |

- 远端同步：已推送 `sliverera/test`（`4be6fa8..272c44f`）；推送后 `测试层` 工作区干净、与远端一致
- 新增功能/变更主体：
  - `EngineTests.exe` 新增主调文件与三种选择模式（`--selector=window` / `console` / `off`）：可图形或控制台选择要执行的测试模块，确认后关闭窗口、恢复控制台输出再运行 googletest；命令行已带 `--gtest_filter` 时透传；
  - 新增测试选择模型（反射建树 + 勾选语义 + 过滤串生成）与 **16 例**模型单元测试，测试规模 263 用例 / 19 套件 → **279 用例 / 20 套件**，无回归；
  - `测试层/external/Dear_ImGui`、`测试层/external/glad` 第三方源码入库（沿用 googletest 惯例）；
  - 跟进引擎层 `Config_Checker` → `Data_Validator` 改名（测试层 44 处引用 + 5 处注释/include），测试文件经 `git mv` 保留历史。
- 项目导航：**已建立** → `项目导航合集/main/测试模块选择器_0555060+272c44f/项目导航.md`

---

## 2026-09-25（测试层：选择器蓝色渐变 + 测试结束后退出等待）

- 顶层仓库版次：**本版次无新提交**（顶层仓库最新仍为 `55e48cd7a5d441f850f1734be8c43a48f00ceb4d`）
- 代码版次（`测试层` 子仓库，分支 `test`；远端 `sliverera/test`）：

| # | 提交哈希 | 提交信息 |
| --- | --- | --- |
| 1 | `cf72e87` | 测试层：选择窗口背景由粉色改为蓝色渐变｜1 file changed / +60 −25 |
| 2 | `05c9994` | 测试层：测试结束后停顿等待按键，不再自动关窗｜3 files changed / +127 −1 |
| 3 | `7f0d4e7` | 测试层：数据校验器空字符串用例转为正式回归用例｜1 file changed / +4 −19 |

- 远端同步：已推送 `sliverera/test`（`272c44f..7f0d4e7`）；推送后 `测试层` 工作区干净、与远端一致
- 新增功能/变更主体：
  - `图形选择窗口.cpp` 配色由紫粉纯色改为「深蓝 → 中蓝 → 亮蓝」垂直渐变：改用背景绘制列表（`AddRectFilledMultiColor` 两段拼接实现三段色），窗口底色 `WindowBg` alpha 归零让渐变透出，其余 `ImGuiCol_*` 与清屏色同步转蓝；仅颜色变化，不改布局与交互；
  - 交互运行时测试结束后打印提示并停住等按键（`等待用户关闭窗口()`）；「要不要等」抽成纯函数 `engine::需要等待退出(选择模式, 命令行带过滤, 显式选择器)`，自动化路径（`--selector=off` / ctest / `--gtest_filter` 透传）跑完即走，退出码不受影响；新增 8 例判定用例覆盖全部分支；
  - 数据校验器空字符串用例由 `DISABLED_` 转正（依赖引擎层 `Data_Validator` 已补的字符串类型与空值检查，该修复由用户在工作区完成，未随本版次提交）。
- 验证：构建成功；`EngineTests.exe --selector=off` **287 / 287 通过**（21 套件，23 DISABLED，exit=0）；`ctest` **1/1 Passed**（0.18 sec）；交互路径起进程 8 秒后仍存活且日志出现等待提示；无控制台环境自动跳过等待、不挂住。规模 279/20 → **287/21**，DISABLED 24 → 23，无回归。
- 项目导航：**已建立** → `项目导航合集/main/选择器蓝色渐变与退出等待_cf72e87+05c9994+7f0d4e7/项目导航.md`
- 未完成项：渐变配色的人眼观感核对（改色后的窗口截图采集失败，仅保留实现级核对）；GUI 鼠标点选交互仍未自动化。

---

## 2026-09-25（引擎层：四叉树坐标类型替换）

- 顶层仓库版次：**本版次无新提交**（顶层仓库最新仍为 `55e48cd7a5d441f850f1734be8c43a48f00ceb4d`；四层为独立仓库，顶层仓库不跟踪其内容）
- 代码版次（`引擎层` 子仓库，分支 `main`，远端 `sliverera/engine`；`测试层` 子仓库，分支 `test`，远端 `sliverera/test`）：

| # | 提交哈希 | 提交信息 |
| --- | --- | --- |
| 1 | `9758826` | 引擎层：四叉树与四叉树管理器替换为新坐标类型｜14 files changed / +211 −220 |
| 2 | `561a915` | 测试层：四叉树相关用例跟进新坐标类型｜2 files changed / +46 −46 |

- 远端同步：已推送 `sliverera/engine`（`00a7c31..9758826`；本地分支 `main` 与上游 `engine` 名字不一致，经显式 refspec `HEAD:engine` 推送）与 `sliverera/test`（`7f0d4e7..561a915`）；推送后两仓与远端一致
- 新增功能/变更主体：
  - 引擎层坐标类型换代为模板实现：`coord2D_int` / `coord2D_double` / `coord2D_range` → `Point2i` / `Point2d` / `Rect2i`（收敛为单层 `namespace engine`，新增 `point_to_int` / `point_to_double`，浮点相等仍走 ULP ≤ 4）；
  - 空间分区两模块共 13 个文件 / 107 处旧类型名全量替换（`Quadtree/` 5 文件、`Quadtree_Manager/` 8 文件），字面替换，算法与控制流零改动；
  - 新类型**取消跨精度隐式转换**，两次构建共暴露 2 处 `Point2d → const Point2i&` 编译错误（`Quadtree/core/区块信息检索.hpp`、`Quadtree_Manager/core/四叉树合并.hpp`），按旧 `coord2D_int` 的 `std::lround` 取整语义各包一层 `point_to_int` 显式化（等价改写）；
  - 测试层 2 个用例文件共 46 处同步替换（`四叉树测试.cpp` 24 处、`四叉树管理器测试.cpp` 22 处），用例结构、构造方式与断言数值不变。
- 验证：引擎层与测试层构建均 exit 0 / `error C` **0**（引擎层以 `-k 0` 全量续跑，产物 `EngineCore.lib` 链接成功）；`EngineTests.exe --selector=off` **287 / 287 通过**（21 套件、23 DISABLED，exit=0；其中 `Quadtree_Test` 35/35、`Quadtree_Manager_Test` 29 例含 1 DISABLED 全 OK）；`ctest` **1/1 Passed**（0.19 sec）；与上一版次基线 287/21/23 完全一致，**无回归**；旧类型名 `coord2D` 在引擎层与测试层源码中**残留 0**。告警仅 2 类既有项（`minwindef.h` C4005、`区块信息检索.hpp(72)` C4715，均非本次引入）。
- 项目导航：**已建立** → `项目导航合集/main/四叉树坐标类型替换_9758826+561a915/项目导航.md`
- 未完成项：`区块信息检索.hpp(72)` 的 C4715 既有告警未处理；新坐标类型自身（ULP 相等、`point_to_int` 取整、`Rect2i` 边界）尚无独立用例，目前仅被四叉树两个套件间接覆盖。

---

## 2026-09-25（引擎层：缺陷修复入库 + 测试层用例转正）

- 顶层仓库版次：**本版次无新提交**（顶层仓库最新仍为 `55e48cd7a5d441f850f1734be8c43a48f00ceb4d`；四层为独立仓库，顶层仓库不跟踪其内容）
- 代码版次（`引擎层` 子仓库，分支 `main`，远端 `sliverera/engine`；`测试层` 子仓库，分支 `test`，远端 `sliverera/test`）：

| # | 层 | 提交哈希 | 提交信息 |
| --- | --- | --- | --- |
| 1 | 引擎层 | `371a4c5` | 修复事件终端、对象池、四叉树、数据校验器与日志系统缺陷｜18 files changed / +1240 −1106 |
| 2 | 测试层 | `8a53e19` | 同步引擎接口变更并补充坐标类型用例｜8 files changed / +532 −259 |
| 3 | 引擎层 | `16d74df` | 修复区块检索在零尺寸下的整数除零｜1 file changed / +3 |
| 4 | 测试层 | `d48362e` | 补充超大边长上限用例的真实崩点说明｜1 file changed / +11 −5 |

- 远端同步：已推送 `sliverera/engine`（`9758826..371a4c5`、`371a4c5..16d74df`）与 `sliverera/test`（`561a915..8a53e19`、`8a53e19..d48362e`）；推送后两仓工作区干净、与远端一致
- 新增功能/变更主体：
  - **事件终端**：`acl_key` 改 `std::optional<int64_t>`，`attach`/`interact`/`send`/`query`/`clear` 统一「密钥未生成即锁定」，`query` 改指针返回（失败 `nullptr`）；
  - **对象池**：`min_valid_index` 显式构造、排序重置改用 `valid()` 重建映射、清空条件由 `if(is_sorted)` 修正为 `if(!is_sorted)`、卸载补 ID 回收与索引维护；
  - **四叉树**：`tree_record::size` 与 `block_seek` 的 `max_size` 回归 `uint64_t`，`child_node_recur` 返回值接入调用处；
  - **四叉树管理器**：`target_range_amend` 改名 + 修正实参顺序 + 补空指针/除零/越界防御与端点裁剪，`set_min_size` 不再误写上限，缓存判满改 `>=` 且加零上限判定，负区间短路，空集建树短路，合并拷贝补空指针检查；
  - **数据校验器**：`if constexpr` 链改 `else if constexpr`（布尔字段不再落入整数分支），字符串分支补类型与空值检查，删除空实现 `.cpp`；
  - **日志系统**：删除全部文件输出重载，改为非静态成员 `stream_set()` 指定活跃流（空串回落控制台）。
- 测试层：**22 条禁用用例转正**（四叉树管理器 10、对象池 10、数据校验器 1、事件终端 1），1 条因未修的整数除零缺陷保留禁用（曾转正试验，重跑仍抛 SEH `0xC0000094`，重新定位后回退并把真实崩点写进注释）；新增坐标类型用例 **21 例**；测试规模 287 / 21 / 23 → **332 / 0 / 1**。
- 验证：引擎层全量重编 `ERR=0`、`EngineCore.lib` 产出（`WARN=17` 全为既有）；测试层构建 exit 0；`EngineTests.exe --selector=off` **332 / 332 通过**（22 套件，1 DISABLED）；`ctest` **1/1 Passed**（0.19 sec）。
- 项目导航：**已建立** → `项目导航合集/main/引擎缺陷修复与用例转正_16d74df+d48362e/项目导航.md`
- 未完成项：超大边长上限下的整数除零（SEH `0xC0000094`）未修——真实崩点在 `Quadtree_Manager/core/区块信息检索.hpp` 的 `excel_element_to_coord`（`width` 为 0），根因是 `int` 型坐标表示无法承载 `INT_MAX` 以上的树尺寸；`Quadtree/core/区块信息检索.hpp(72)` 既有 `warning C4715` 未处理。

---

## 2026-09-25（引擎层 + 测试层：四叉树链路尺寸层 64 位化）

- 顶层仓库版次：**本版次无新提交**（顶层仓库最新仍为 `55e48cd7a5d441f850f1734be8c43a48f00ceb4d`；四层为独立仓库，顶层仓库不跟踪其内容）
- 代码版次（`引擎层` 子仓库，分支 `main`，远端 `sliverera/engine`；`测试层` 子仓库，分支 `test`，远端 `sliverera/test`）：

| # | 层 | 提交哈希 | 提交信息 |
| --- | --- | --- | --- |
| 1 | 引擎层 | `fa8459a` | 四叉树链路尺寸层 64 位化，修复超大边长上限整数除零｜12 files changed / +284 −184 |
| 2 | 测试层 | `4c7f164` | 超大边长上限用例转正，补 64 位坐标用例｜3 files changed / +133 −42 |

- 远端同步：已推送 `sliverera/engine`（`16d74df..fa8459a`，本地分支 `main` 与上游 `engine` 名字不一致，经显式 refspec `main:engine`）与 `sliverera/test`（`d48362e..4c7f164`）；推送后两仓工作区干净、与远端一致。按用户要求未走 PR。
- 新增功能/变更主体：
  - **尺寸层分层契约（本版次核心）**：新增 `Point2l` / `Rect2l`（`Point2<int64_t>` / `Rect2<int64_t>`）与 `point_to_l`（`std::llround`，口径同 `point_to_int`）、`point_to_double(Point2l)` 重载；**对外公开接口保持 `Point2i` / `Rect2i` 不变**（`seek` 单点与范围、`qurdtree_build_smart`、`set_max_size` / `set_min_size`、`coord_set`），模块内部的树边界范围、由尺寸推导的坐标、待查询区块坐标、树间范围计算、`tree_cache.ranges` 与回调契约 `target` 换 `Point2l` / `Rect2l`。判据：**能由「树尺寸」推导出来的坐标一律 64 位，来自外界的世界坐标保持 32 位**——因此系统层、游戏层与全项目调用方零改动；
  - **主崩点修复**：`Quadtree_Manager/core/四叉树智能创建.hpp` 的 `father_block_size` 由 `int` 改 `int64_t`，并补 `father_block_size <= 0`、`max_block_num_total <= 0 || max_block_size <= 0` 两道早退；尺寸与区块索引系列全部 64 位。该崩点已用最小复现程序实测钉死（`2^33 stored into int = 0` → `1 % 0` → `run_exit = 0xC0000094`），并据此更正上一版次注释：真实主崩点是 `prepare_smart_create_params` 的 int 截断取模，`excel_element_to_coord` 是同源隐患；
  - **零尺寸与溢出防御**：`excel_element_to_coord` 补 `block_size == 0` / `width <= 0` 早退，`target_range_format` 补 `block_size == 0` 早退，`range_seek` 的 `total_num` 提升并做 `want_row > INT64_MAX / want_col` 乘法溢出检查（越界直接返回）；
  - **两模块共 12 文件同步**：`Quadtree/`（`函数预声明.h`、`core/导航与范围计算.hpp`、`core/区块信息检索.hpp`、`core/设置与交互.hpp`）＋ `Quadtree_Manager/`（`函数预声明.h`、`core/` 下 6 个 hpp）；其中 `manage_range_calcu` 全程 int64 运算、`child_node_range_calcu` 中点改 int64、`recur_direct_calcu` 的 `/2.0f` 改 `/2.0`（消除原地舍入误差）、`四叉树扩大回调管理` 的 `min_size` 改 `int64_t` 并去掉 `*= -1` 的无符号写法、`四叉树合并` 两处改 `point_to_l`；
  - **测试层**：`Quadtree_Manager_Test.超大边长上限下区块检索次数正常` 去掉 `DISABLED_` 前缀转正（`set_max_size(1ull << 33)` → `qurdtree_build_smart` → 断言 1 条记录 → 稳定 `seek` → `chunk_clean`），头部注释按实测崩点重写；`四叉树测试.cpp` 18 处类型同步（4 处 lambda 形参 ＋ 14 处局部量）；`坐标类型测试.cpp` 新增 5 例 64 位用例（别名语义与读写、超 int 边界值承载、`point_to_l` 取整口径、经 `point_to_double` 往返一致）。
- 验证：引擎层与测试层构建均 exit 0 / `error C` **0**（`EngineCore.lib`、`EngineTests.exe` 产出；告警全部为既有项：C4005 ×12、C4715 ×3、C4828 ×2）；`EngineTests.exe --selector=off` **338 / 338 通过 / 0 failed / 0 DISABLED**（22 套件，exit 0；`Quadtree_Manager_Test` 40、`Quadtree_Test` 35、`Coord_Type_Test` 26 全 OK）；`ctest` **1/1 Passed**（0.19 sec）；旧类型名 `coord2D` 残留 **0**；测试规模 332 / 0 / 1 → **338 / 0 / 0**。
- 项目导航：**已建立** → `项目导航合集/main/超大上限尺寸层64位化_fa8459a+4c7f164/项目导航.md`
- 未完成项：超大树上的范围查询未验证也未承诺（内存物理限制，本版次只承诺建树与单点检索）；对外世界坐标仍受 `int`（±2^31）约束；`Quadtree/core/区块信息检索.hpp(72)` 与 `Event_Broker/core/事件中转器.cpp(221)` 的既有 `warning C4715` 未处理。

---

## 2026-09-25（引擎层：碰撞模块单文件拆分为多编译单元）

- 顶层仓库版次：**本版次无新提交**（四层为独立仓库，顶层仓库不跟踪其内容；顶层最新仍为 `55e48cd7a5d441f850f1734be8c43a48f00ceb4d`）
- 代码版次（`引擎层` 子仓库，分支 `main`，远端 `sliverera/engine`；**测试层本版次无改动、未提交**）：

| # | 层 | 提交哈希 | 提交信息 |
| --- | --- | --- | --- |
| 1 | 引擎层 | `089b070` | 拆分碰撞代理器与碰撞空间为 9 个编译单元｜11 files changed / +1835 −1756 |

- 远端同步：已推送 `sliverera/engine`（`fa8459a..089b070`，本地分支 `main` 与上游 `engine` 名字不一致，经显式 refspec `main:engine`）；推送后引擎层工作区干净、与远端一致。按用户要求未走 PR。
- 新增功能/变更主体：
  - **删除**：`Collision_Proxy/core/碰撞代理器.cpp`（919 行）、`Collision_Region/core/碰撞空间.cpp`（839 行）；
  - **Collision_Proxy 新增 5 单元**：`空间管理.cpp`(166) / `空间边界与配置.cpp`(177) / `碰撞体管理.cpp`(200) / `碰撞体配置.cpp`(153) / `事件交互.cpp`(272)；
  - **Collision_Region 新增 4 单元**：`空间与边界.cpp`(108) / `碰撞体管理.cpp`(279) / `检测执行.cpp`(174) / `形状构建.cpp`(306)；
  - **搬运口径**：函数体、注释与 `Log::warn` / `Log::error` / `Log::debug` 文本逐字未改；每个单元只加最小 include（`../局部命名空间使用.h` ＋ 实际用到的工具头）；匿名命名空间辅助随其使用者同文件，**未提升任何可见性**；
  - **一次分组假设被实测推翻**：`text_read` / `number_read` 的使用者横跨三个单元（`event_process` 也在用，8 处），匿名命名空间实体跨 TU 不可见 → 构建报 8 条 `C3861`；按「不提高辅助可见性」的既定原则，改为由 `事件交互.cpp` 自带副本（仍置于匿名命名空间内）；
  - **一次补丁事故**：插入辅助时漏带匿名命名空间闭合 `}`，`attach` / `event_process` / `event_publish` 被卷入匿名命名空间（6 条 `C2888` / `C1075`）→ 放弃增量修补，修正生成脚本后整批重生成 9 个文件。生成脚本已获改造：原文件不在工作区时用 `git -C <引擎层> show HEAD:<relpath>` 取回原文，可完整复现 9 个文件；
  - **对外零变化**：两个模块的 `.h` 与两个 `局部命名空间使用.h` 一字未动；CMakeLists 未改（`GLOB_RECURSE CONFIGURE_DEPENDS` 自动纳入新 `.cpp`）；测试层 / 系统层 / 游戏层零改动。
- 验证：引擎层 `configure exit=0` / `build exit=0` / `error C` **0**（`EngineCore.lib` 产出，`[128/129] Linking CXX static library`）；测试层 `build exit=0`；`EngineTests.exe --selector=off` **338 / 338 通过 / 0 failed / 0 DISABLED**（22 套件，exit 0；`Collision_Region_Test` 9 例、`Collision_Proxy_Test` 8 例全 OK）；`ctest` **1/1 Passed**（0.19 sec）；**`EngineCore.lib` 外部符号集前后均 16342 条、`diff` 零差异**（同规则双向复现，排除提取偏差）；逐行搬运覆盖自检：Proxy 非空 822 / 区间 885、Region 非空 748 / 区间 799，不重不漏。告警：C4715 ×3、C4828 ×2 与基线逐条一致；C4005 由 12 → 19，与该警告「每编译单元各报一次」及编译单元数 120 → 127 一一对应（既知同源告警，非新增问题）。
- 项目导航：**已建立** → `项目导航合集/main/碰撞模块单文件拆分为多编译单元_089b070/项目导航.md`；2026-09-26 重建为**完整结构**：`项目架构.md`（36KB）+ `模块架构/` 20 个模块单一文件（25KB~263KB，覆盖引擎层/测试层/系统层/游戏层全部模块与构建契约）
- 未完成项：`形状构建.cpp` 306 行、超计划上限 6 行未挪动；9 个单元行数未二次匀化；`事件交互.cpp` 的辅助副本未消重（消重需引入模块内部共享头）；上一版次遗留的 `Quadtree/core/区块信息检索.hpp(72)` 与 `Event_Broker/core/事件中转器.cpp(221)` 的既有 `warning C4715` 未处理。

---

## 历史说明

- 本文件随 2026-09-19「工作站建立」任务首次建立（项目：白银纪元）。
- 本文件只维护与「项目导航」文件夹版次对应的顶层仓库摘要；四层子仓库的完整提交时间线保存在各自仓库内，可通过 `git log` 查看。
- 本项目下的「游戏引擎」子项目（现位于 `系统层/`）在独立成层之前，曾于 `game_engine` 工作站留有版次记录（基于搬迁前的旧工作区路径与版次 `0b8dbd27…`），仅作历史参考，不作为本工作站的有效版次。
