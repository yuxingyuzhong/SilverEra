#pragma once
//============================================================================
// 配置编辑器主程序 —— 外观（共享声明）
// 由 配置编辑器主程序.cpp 的匿名命名空间外观函数拆分而来（架构改革 阶段 4）
// 原外观函数位于匿名命名空间，拆分后改为 engine 命名空间共享函数，行为与拆分前完全一致。
//============================================================================
#include "common/前置头文件包含.h"

//引擎命名空间
namespace engine
{
    //收集候选字体路径（按优先级，跨平台回退）
    std::vector<std::string> 收集候选字体路径();
    //尝试加载中文字体（失败返回 nullptr）
    ImFont* 加载中文字体(float 像素大小);
    //应用梦幻粉色主题（明丽版）
    void 应用粉色主题();
    //渲染梦幻背景（粉紫渐变 + 闪烁星光）
    void 渲染梦幻背景();
}
